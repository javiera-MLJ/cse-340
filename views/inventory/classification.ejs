<%# A content heading %>
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>

<% if (notice) { %>
    <div class="notice"><%= notice %></div>
<% } %>

<%# inventory items display %>
<%- grid %>